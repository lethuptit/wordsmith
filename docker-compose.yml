version: '3.9'
# we'll keep the version for now to work in Compose and Swarm

services:
  db:
    image: postgres:10.0-alpine
    volumes:
      - ./db/words.sql:/docker-entrypoint-initdb.d/words.sql

  api:
    build: api
    image: dockersamples-test/wordsmith-test-api
    deploy:
      replicas: 5

  web:
    build: web
    image: dockersamples/wordsmith-web
    ports:
     - "8080:80"